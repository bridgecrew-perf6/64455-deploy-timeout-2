import { regions, regionResolvers } from '@base/config/regions';

import BlockRegion from '@shop/components/Page/Regions/Block';

regions.set('region.block', BlockRegion);

export { regions, regionResolvers };
